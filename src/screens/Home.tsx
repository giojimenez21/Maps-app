import { BtnLocation, MapView, SearchBar } from "../components"

export const Home = () => {
    return (
        <div>
            <MapView />
            <BtnLocation />
            <SearchBar />
        </div>
    )
}
